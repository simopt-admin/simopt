name: "ruff format checking"

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  lint:
    runs-on: ubuntu-latest 
    steps:
    - name: Run Ruff format checking
      uses: astral-sh/ruff-action@v2
      with:
        args: "format --check ."
        src: "./simopt"
